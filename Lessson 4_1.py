class RegirsterForm:

    def __int__(self, username, password):
        self.username = username
        self.password = password
    @classmethod
    def is_valid(cls):
        if not isinstance(self.username, srt):
            return False
        elif len(self.username) < 2
            return False
        if not isinstance(self.password, srt):
            return False
        elif len(self.password) < 2
            return False



